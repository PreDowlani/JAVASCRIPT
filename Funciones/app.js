// ==== Funciones en JavaScript ====
// nombre = 'René';
// apellidos = 'Martín Cáceres';
// nombreCompleto = nombre + ' ' + apellidos;

// Sintaxis: function nombreFuncion (parametros) {codigo; return}
// Function Declaration (Declaración de la función)
function nomCompleto(nombre, apellidos) {
  completo = nombre + " " + apellidos;
  return completo;
}

console.log(nomCompleto('Pre', 'Dowlani'));
nombreCom = nomCompleto('Pre', 'Dowlani');

// Function Expression
const nombreCompletoDeNuevo = function (nombre, apellidos) {
  completo = nombre + " " + apellidos;
  return completo;
};

// const nombreUsuario = [];
// nombre2 = "Yudi";
// apellidos2 = "Melián Sánchez";
// nombreCliente = nomCompleto(nombre2, apellidos2);
// console.log(nombreCliente);

// nombreUsuario[0] = nomCompleto("Fran", "Dávila");
// console.log(nombreUsuario);

// console.log(nombreCompletoDeNuevo("Daniel", "Ojeda"));

for (let i = 0; i < array.length; i++) {
  nombre[i].impuestos = calcularImpuestos(id, id2);
}

// nombreUsuario.splice(1, 3);

function splice(valor1, valor2) {
  // cortar el array y el resultado lo asigna a 'resultado'
  return resultado;
}

// // Comunicación entre funciones
// // Llamadas entre funciones

function comprobandoLogin() {
  console.log("Comprobando...");
}

function inicioLogin() {
  console.log("Introduzca nombre");
  comprobandoLogin();
}

inicioLogin();


